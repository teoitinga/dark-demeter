import { MascaraDirectiveDirective } from './mascara-directive.directive';

describe('MascaraDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MascaraDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
